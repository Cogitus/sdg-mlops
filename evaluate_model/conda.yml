name: evaluate_model
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  - python=3.10
  - pip:
      - numpy
      - pandas
      - scikit-learn
      - tensorflow
      - mlflow
      - wandb
