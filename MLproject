name: SDG multilabel classification
conda_env: main_environment.yml

entry_points:
    main:
        parameters:
            overriding_configs:
                # TODO
                description: >
                    These are the string-like override methods used by Hydra to 
                    change the default configurations of the pipeline.
                type: str
                default: ''
        command:
            python3 main.py  $(echo {overriding_configs})

    download_data:
        parameters:
            overriding_configs:
                # TODO
                description: >
                    These are the string-like override methods used by Hydra to 
                    change the default configurations of the pipeline.
                type: str
                default: ''
        command:
            python3 ./download_data/main.py $(echo {overriding_configs})  

    preprocess_data:
        parameters:
            # TODO
            description: >
                These are the string-like override methods used by Hydra to 
                change the default configurations of the pipeline.
            type: str
            default: ''
        command:
            python3 ./preprocess_data/main.py $(echo {overriding_configs})
    
    split_data:
        parameters:
            # TODO
            description: >
                These are the string-like override methods used by Hydra to 
                change the default configurations of the pipeline.
            type: str
            default: ''
        command:
            python3 ./split_data/main.py $(echo {overriding_configs})
    
    train:
        parameters:
            # TODO
            description: >
                These are the string-like override methods used by Hydra to 
                change the default configurations of the pipeline.
            type: str
            default: ''
        command:
            python3 ./train/main.py $(echo {overriding_configs})
    
    evaluate_model:
        parameters:
            # TODO
            description: >
                These are the string-like override methods used by Hydra to 
                change the default configurations of the pipeline.
            type: str
            default: ''
        command:
            python3 ./evaluate_model/main.py $(echo {overriding_configs})